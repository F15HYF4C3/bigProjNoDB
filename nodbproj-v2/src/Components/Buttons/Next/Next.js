import React from 'react';
import './Next.css';




const Next = (props) => {
    
        return (

          <button className="movement" onClick={props.displayNext}>
          NEXT
        </button>
      
        );
      }
      

export default Next