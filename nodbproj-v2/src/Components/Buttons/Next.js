import React from 'react';
import './buttons.css';




const Next = (props) => {
    
        return (

          <button onClick={props.displayNext}>
          NEXT
        </button>
      
        );
      }
      

export default Next